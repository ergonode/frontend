/*
 * Copyright © Bold Brand Commerce Sp. z o.o. All rights reserved.
 * See LICENSE for license details.
 */
<%= options.extend.map(path =>`import ${'extend_' + hash(path)} from '${path.replace(/\\/g,'/')}';`).join('\n') %>

export default {
<%= options.extend.map(path =>`\t\textend_${hash(path)},`).join('\n') %>
};
