/*
 * Copyright © Bold Brand Commerce Sp. z o.o. All rights reserved.
 * See LICENSE for license details.
 */
export default {
<%= Object.keys(options.configuration).map((key) =>`\t\t'${key}': ${JSON.stringify(options.configuration[key])},`).join('\n') %>
};
