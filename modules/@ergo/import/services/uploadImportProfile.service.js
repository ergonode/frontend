/*
 * Copyright © Bold Brand Commerce Sp. z o.o. All rights reserved.
 * See LICENSE for license details.
 */
import { getKeyByValue } from '@Core/models/objectWrapper';

export default function ({ $axios, $store }) {
    // const { language } = $store.state.authentication.user;
    // const { name, type } = $store.state.import;
    // const { sources } = $store.state.dictionaries;
    // const typeId = getKeyByValue(sources, type);
    // // /api/v1/{language}/imports/upload
    // const formData = new FormData();
    // formData.append('upload', file, name);
    //
    // return $axios.$post(`${language}/imports/upload`, { type: typeId, name });
}
