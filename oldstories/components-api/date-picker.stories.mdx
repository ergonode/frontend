import { Meta, Props } from '@storybook/addon-docs/blocks';
import DatePicker from '@UI/components/DatePicker/DatePicker';

<Meta title="Components API|DatePicker" />

# DatePicker

`DatePicker` is a default date picker component. It might be configured with `prepend` and  `append` slots.

```
    import DatePicker from '@UI/components/DatePicker/DatePicker'
```

### Props

<Props of={DatePicker} />
