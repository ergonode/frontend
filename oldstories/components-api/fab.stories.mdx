import { Meta, Story, Preview, Description, Props } from '@storybook/addon-docs/blocks';
import Fab from '@UI/components/Fab/Fab';
import IconAdd from '@UI/components/Icons/Actions/IconAdd';
import { SIZE, THEME } from '@Core/defaults/theme';

<Meta title="Components API|Fab" />

# Fab

<Description of={Fab} />

```
    import Fab from '@UI/components/Fab/Fab'
```

### Theme

<Preview mdxSource={`
    <Fab theme="${THEME.PRIMARY}">
        <template #icon="{ color, isHovered }">
            <IconAdd :fill-color="color" />
        </template>
    </Fab>
    <!-- -->
    <Fab theme="${THEME.SECONDARY}">
        <template #icon="{ color, isHovered }">
            <IconAdd :fill-color="color" />
        </template>
    </Fab>
    <!-- -->
    <Fab theme="${THEME.SECONDARY_PLAIN}">
        <template #icon="{ color, isHovered }">
            <IconAdd :fill-color="color" />
        </template>
    </Fab>
`}>
    <Story name="primary">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="primaryTheme">
                    <template #icon="{ color, isHovered }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Fab>
            `,
            computed: {
                primaryTheme() {
                    return THEME.PRIMARY;
                }
            }
        }}
    </Story>
    <Story name="secondary">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="secondaryTheme">
                    <template #icon="{ color, isHovered }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Fab>
            `,
            computed: {
                secondaryTheme() {
                    return THEME.SECONDARY;
                }
            }
        }}
    </Story>
    <Story name="secondary-plain">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="secondaryPlainTheme">
                    <template #icon="{ color, isHovered }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Fab>
            `,
            computed: {
                secondaryPlainTheme() {
                    return THEME.SECONDARY_PLAIN;
                }
            }
        }}
    </Story>
</Preview>

### Disabled

<Preview mdxSource={`
    <Fab :disabled="true" theme="${THEME.PRIMARY}">
        <template #icon="{ color, isHovered }">
            <IconAdd :fill-color="color" />
        </template>
    </Fab>
    <!-- -->
    <Fab :disabled="true" theme="${THEME.SECONDARY}">
        <template #icon="{ color, isHovered }">
            <IconAdd :fill-color="color" />
        </template>
    </Fab>
    <!-- -->
    <Fab :disabled="true" theme="${THEME.SECONDARY_PLAIN}">
        <template #icon="{ color, isHovered }">
            <IconAdd :fill-color="color" />
        </template>
    </Fab>
`}>
    <Story name="disabled-primary">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :disabled="true" :theme="primaryTheme">
                    <template #icon="{ color, isHovered }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Fab>
            `,
            computed: {
                primaryTheme() {
                    return THEME.PRIMARY;
                }
            }
        }}
    </Story>
    <Story name="disabled-secondary">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :disabled="true" :theme="secondaryTheme">
                    <template #icon="{ color, isHovered }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Fab>
            `,
            computed: {
                secondaryTheme() {
                    return THEME.SECONDARY;
                }
            }
        }}
    </Story>
    <Story name="disabled-secondary-plain">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :disabled="true" :theme="secondaryPlainTheme">
                    <template #icon="{ color, isHovered }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Fab>
            `,
            computed: {
                secondaryPlainTheme() {
                    return THEME.SECONDARY_PLAIN;
                }
            }
        }}
    </Story>
</Preview>

### Size
<Preview mdxSource={`
    <Fab theme="${THEME.PRIMARY}" size="${SIZE.REGULAR}">
        <template #icon="{ color, isHovered }">
            <IconAdd :fill-color="color" />
        </template>
    </Fab>
    <!-- -->
    <Fab theme="${THEME.PRIMARY}" size="${SIZE.SMALL}">
        <template #icon="{ color, isHovered }">
            <IconAdd :fill-color="color" />
        </template>
    </Fab>
    <!-- -->
    <Fab theme="${THEME.PRIMARY}" size="${SIZE.TINY}">
        <template #icon="{ color, isHovered }">
            <IconAdd :fill-color="color" />
        </template>
    </Fab>
`}>
    <Story name="regular">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="primaryTheme" :size="regularSize">
                    <template #icon="{ color, isHovered }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Fab>
            `,
            computed: {
                primaryTheme() {
                    return THEME.PRIMARY;
                },
                regularSize() {
                    return SIZE.REGULAR;
                },
            }
        }}
    </Story>
    <Story name="small">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="primaryTheme" :size="smallSize">
                    <template #icon="{ color, isHovered }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Fab>
            `,
            computed: {
                primaryTheme() {
                    return THEME.PRIMARY;
                },
                smallSize() {
                    return SIZE.SMALL;
                },
            }
        }}
    </Story>
    <Story name="tiny">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="primaryTheme" :size="tinySize">
                    <template #icon="{ color, isHovered }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Fab>
            `,
            computed: {
                primaryTheme() {
                    return THEME.PRIMARY;
                },
                tinySize() {
                    return SIZE.TINY;
                },
            }
        }}
    </Story>
</Preview>

### Props

<Props of={Fab} />
