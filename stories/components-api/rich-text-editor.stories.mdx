import { Meta, Props } from '@storybook/addon-docs/blocks';
import RichTextEditor from '@Core/components/RichTextEditor/RichTextEditor';

<Meta title="Components API|RichTextEditor" />

# RichTextEditor

`RichTextEditor` is a default rich content editor component. It might be configured with `prepend` and  `append` slots.

```
    import RichTextEditor from '@Core/components/RichTextEditor/RichTextEditor'
```

### Props

<Props of={RichTextEditor} />
