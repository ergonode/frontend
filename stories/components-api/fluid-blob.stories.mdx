import { Meta, Props, Preview, Story } from '@storybook/addon-docs/blocks';
import FluidBlob from '@UI/components/FluidBlob/FluidBlob';

<Meta title="Components API|FluidBlob" />

# FluidBlob

`FluidBlob` is a fluid blob component.

```
    import FluidBlob from '@UI/components/FluidBlob/FluidBlob';
```

### Props

<Props of={FluidBlob} />

<Preview mdxSource={`
    <FluidBlob />
`}>
    <Story name="fludi-blob">
        {{
            components: { FluidBlob },
            template: `
                <FluidBlob />
            `
        }}
    </Story>
</Preview>
