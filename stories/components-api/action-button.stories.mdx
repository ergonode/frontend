import { Meta, Story, Preview, Description, Props } from '@storybook/addon-docs/blocks';
import { THEMES, SIZES } from '@Core/defaults/buttons';
import ActionButton from '@Core/components/Buttons/ActionButton';

<Meta title="Components API|ActionButton" />

# ActionButton

`ActionButton` is an `ActionBaseButton` with a `Button` inside named slot `button`. It's implementing `Button` props.

```
    import ActionButton from '@Core/components/Buttons/ActionButton'
```

### Props
<Props of={ActionButton} />
