import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Button from '@Core/components/Buttons/Button';
import IconAdd from '@Core/components/Icons/Actions/IconAdd';
import { SIZE, THEME } from '@Core/defaults/theme';

<Meta title="Components API|Button" />

# Button

`Button` is a default component to display action in page. It may have any icon component inside `prepend` and `append` slots.

```
    import Button from '@Core/components/Buttons/Button'
```

### Theme

<Preview>
    <Story name="primary">
        {{
            components: { Button },
            template: `<Button title="PRIMARY" :theme="primaryTheme" />`,
            computed: {
                primaryTheme() {
                    return THEME.PRIMARY;
                }
            }
        }}
    </Story>
    <Story name="secondary">
        {{
            components: { Button },
            template: `<Button title="SECONDARY" :theme="secondaryTheme" />`,
            computed: {
                secondaryTheme() {
                    return THEME.SECONDARY;
                }
            }
        }}
    </Story>
</Preview>

### Disabled

<Preview>
    <Story name="disabled-primary">
        {{
            components: { Button },
            template: `<Button title="PRIMARY" :theme="primaryTheme" :disabled="true" />`,
            computed: {
                primaryTheme() {
                    return THEME.PRIMARY;
                }
            }
        }}
    </Story>
    <Story name="disabled-secondary">
        {{
            components: { Button },
            template: `<Button title="SECONDARY" :theme="secondaryTheme" :disabled="true" />`,
            computed: {
                secondaryTheme() {
                    return THEME.SECONDARY;
                }
            }
        }}
    </Story>
</Preview>

### Size

<Preview>
    <Story name="regular">
        {{
            components: { Button },
            template: `<Button title="PRIMARY" :theme="primaryTheme" :size="regularSize" />`,
            computed: {
                primaryTheme() {
                    return THEME.PRIMARY;
                },
                regularSize() {
                    return SIZE.REGULAR;
                },
            }
        }}
    </Story>
    <Story name="small">
        {{
            components: { Button },
            template: `<Button title="PRIMARY" :theme="primaryTheme" :size="smallSize" />`,
            computed: {
                primaryTheme() {
                    return THEME.PRIMARY;
                },
                smallSize() {
                    return SIZE.SMALL;
                },
            }
        }}
    </Story>
    <Story name="tiny">
        {{
            components: { Button },
            template: `<Button title="PRIMARY" :theme="primaryTheme" :size="tinySize" />`,
            computed: {
                primaryTheme() {
                    return THEME.PRIMARY;
                },
                tinySize() {
                    return SIZE.TINY;
                },
            }
        }}
    </Story>
</Preview>

### Slots

<Preview>
    <Story name="append">
        {{
            components: { Button, IconAdd },
            template: `
                <Button title="APPEND">
                    <template #append="{ color }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Button>
            `,
        }}
    </Story>
    <Story name="prepend">
        {{
            components: { Button, IconAdd },
            template: `
                <Button title="PREPEND">
                    <template #prepend="{ color }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Button>
            `,
        }}
    </Story>
    <Story name="append-prepend">
        {{
            components: { Button, IconAdd },
            template: `
                <Button title="APPEND | PREPEND">
                    <template #append="{ color }">
                        <IconAdd :fill-color="color" />
                    </template>
                    <template #prepend="{ color }">
                        <IconAdd :fill-color="color" />
                    </template>
                </Button>
            `,
        }}
    </Story>
</Preview>

### Props

<Props of={Button} />
