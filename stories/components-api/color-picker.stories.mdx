import { Meta, Props } from '@storybook/addon-docs/blocks';
import ColorPicker from '@Core/components/ColorPicker/ColorPicker';

<Meta title="Components API|ColorPicker" />

# ColorPicker

`ColorPicker` is a default color options component. It might be configured with `prepend` and `append` slots.

```
    import ColorPicker from '@Core/components/ColorPicker/ColorPicker'
```

### Props

<Props of={ColorPicker} />
