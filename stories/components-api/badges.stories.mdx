import { Meta, Props } from '@storybook/addon-docs/blocks';
import BadgeLabel from '@UI/components/Badges/BadgeLabel';
import InformationBadge from '@UI/components/Badges/InformationBadge';
import InformationIconBadge from '@UI/components/Badges/InformationIconBadge';
import NotificationBadge from '@UI/components/Badges/NotificationBadge';
import NumericBadge from '@UI/components/Badges/NumericBadge';
import PointBadge from '@UI/components/Badges/PointBadge';
import RoundedBadge from '@UI/components/Badges/RoundedBadge';

<Meta title="Components API|Badges" />

# Badges

Used for giving user feedback about color, state or simple information.

## BadgeLabel

`BadgeLabel` is a label for badge components.

```
    import BadgeLabel from '@UI/components/Badges/BadgeLabel';
```

### Props

<Props of={BadgeLabel} />

## InformationBadge

`InformationBadge` is a default badge information component for giving user written feedback about something.

```
    import InformationBadge from '@UI/components/Badges/InformationBadge';
```

### Props

<Props of={InformationBadge} />

## InformationIconBadge

`InformationIconBadge` is a default badge information component for giving user written feedback with addition of an icon about something.

```
    import InformationIconBadge from '@UI/components/Badges/InformationIconBadge';
```

### Props

<Props of={InformationIconBadge} />

## NotificationBadge

`NotificationBadge` is a badge component which is giving user feedback about number of unread notification. It's getting rounded information about number of unread notifications greater than 99

```
    import NotificationBadge from '@UI/components/Badges/NotificationBadge';
```

### Props

<Props of={NotificationBadge} />

## NumericBadge

`NumericBadge` is a badge component which is giving user numeric feedback.

```
    import NumericBadge from '@UI/components/Badges/NumericBadge';
```

### Props

<Props of={NumericBadge} />

## PointBadge

`PointBadge` is a badge component indicating colored state of something.

```
    import PointBadge from '@UI/components/Badges/PointBadge';
```

### Props

<Props of={PointBadge} />

## RoundedBadge

`RoundedBadge` is a badge component used as a wrapper for giving rounded badges.

```
    import RoundedBadge from '@UI/components/Badges/RoundedBadge';
```

### Props

<Props of={RoundedBadge} />
