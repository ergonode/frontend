import { Meta, Props } from '@storybook/addon-docs/blocks';
import Toggler from '@Core/components/Toggler/Toggler';

<Meta title="Components API|Toggler" />

# Toggler

`Toggler` is a default switch component. It might be configured with `append` slots.

```
    import Toggler from '@Core/components/Toggler/Toggler'
```

### Props

<Props of={Toggler} />
