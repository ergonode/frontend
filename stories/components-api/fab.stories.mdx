import { Meta, Story, Preview, Description, Props } from '@storybook/addon-docs/blocks';
import Fab from '@Core/components/Buttons/Fab';
import IconAdd from '@Core/components/Icons/Actions/IconAdd';
import { THEMES, SIZES } from '@Core/defaults/buttons';

<Meta title="Components API|Fab" />

# Fab

`Fab` is a default component to display action in page. It may have any icon component inside `icon` slot.

```
    import Fab from '@Core/components/Buttons/Fab'
```

### Theme

<Preview>
    <Story name="primary">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="primaryTheme">
                    <template #icon="{ fillColor, isHovered }">
                        <IconAdd :fill-color="fillColor" />
                    </template>
                </Fab>
            `,
            computed: {
                primaryTheme() {
                    return THEMES.PRIMARY;
                }
            }
        }}
    </Story>
    <Story name="secondary">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="secondaryTheme">
                    <template #icon="{ fillColor, isHovered }">
                        <IconAdd :fill-color="fillColor" />
                    </template>
                </Fab>
            `,
            computed: {
                secondaryTheme() {
                    return THEMES.SECONDARY;
                }
            }
        }}
    </Story>
    <Story name="secondary-plain">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="secondaryPlainTheme">
                    <template #icon="{ fillColor, isHovered }">
                        <IconAdd :fill-color="fillColor" />
                    </template>
                </Fab>
            `,
            computed: {
                secondaryPlainTheme() {
                    return THEMES.SECONDARY_PLAIN;
                }
            }
        }}
    </Story>
</Preview>

### Disabled

<Preview>
    <Story name="disabled-primary">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :disabled="true" :theme="primaryTheme">
                    <template #icon="{ fillColor, isHovered }">
                        <IconAdd :fill-color="fillColor" />
                    </template>
                </Fab>
            `,
            computed: {
                primaryTheme() {
                    return THEMES.PRIMARY;
                }
            }
        }}
    </Story>
    <Story name="disabled-secondary">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :disabled="true" :theme="secondaryTheme">
                    <template #icon="{ fillColor, isHovered }">
                        <IconAdd :fill-color="fillColor" />
                    </template>
                </Fab>
            `,
            computed: {
                secondaryTheme() {
                    return THEMES.SECONDARY;
                }
            }
        }}
    </Story>
    <Story name="disabled-secondary-plain">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :disabled="true" :theme="secondaryPlainTheme">
                    <template #icon="{ fillColor, isHovered }">
                        <IconAdd :fill-color="fillColor" />
                    </template>
                </Fab>
            `,
            computed: {
                secondaryPlainTheme() {
                    return THEMES.SECONDARY_PLAIN;
                }
            }
        }}
    </Story>
</Preview>

### Size

<Preview>
    <Story name="regular">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="primaryTheme" :size="regularSize">
                    <template #icon="{ fillColor, isHovered }">
                        <IconAdd :fill-color="fillColor" />
                    </template>
                </Fab>
            `,
            computed: {
                primaryTheme() {
                    return THEMES.PRIMARY;
                },
                regularSize() {
                    return SIZES.REGULAR;
                },
            }
        }}
    </Story>
    <Story name="small">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="primaryTheme" :size="smallSize">
                    <template #icon="{ fillColor, isHovered }">
                        <IconAdd :fill-color="fillColor" />
                    </template>
                </Fab>
            `,
            computed: {
                primaryTheme() {
                    return THEMES.PRIMARY;
                },
                smallSize() {
                    return SIZES.SMALL;
                },
            }
        }}
    </Story>
    <Story name="tiny">
        {{
            components: { Fab, IconAdd },
            template: `
                <Fab :theme="primaryTheme" :size="tinySize">
                    <template #icon="{ fillColor, isHovered }">
                        <IconAdd :fill-color="fillColor" />
                    </template>
                </Fab>
            `,
            computed: {
                primaryTheme() {
                    return THEMES.PRIMARY;
                },
                tinySize() {
                    return SIZES.TINY;
                },
            }
        }}
    </Story>
</Preview>

### Props

<Props of={Fab} />
