import { Meta, Story, Preview, Description, Props } from '@storybook/addon-docs/blocks';
import { THEMES, SIZES } from '@Core/defaults/buttons';
import Alert from '@Core/components/Alerts/Alert';

<Meta title="Components API|Alert" />

# Alert

`Alert` is a default component for giving feedback for user on http response. Alerts are going to be displayed as default `FlashMessage`.

```
    import Alert from '@Core/components/Alerts/Alert'
```

### Props
<Props of={Alert} />
