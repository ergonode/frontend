import { Meta, Story, Preview, Props, Description } from '@storybook/addon-docs/blocks';
import { ALERT_TYPE } from '@Core/defaults/alerts';
import Alert from '@Core/components/Alerts/Alert';

<Meta title="Components API|Alert" />

# Alert

<Description of={Alert} />

```
    import Alert from '@Core/components/Alerts/Alert'
```

### Types

<Preview mdxSource={`
    <Alert :type="${ALERT_TYPE.SUCCESS}" message="That is success Alert" />
    <!-- -->
    <Alert :type="${ALERT_TYPE.INFO}" message="That is info Alert" />
    <!-- -->
    <Alert :type="${ALERT_TYPE.WARNING}" message="That is warning Alert" />
    <!-- -->
    <Alert :type="${ALERT_TYPE.ERROR}" message="That is error Alert" />
`}>
    <Story name="success">
        {{
            components: { Alert },
            template: `<Alert id="1" :type="type" :message="message" />`,
            computed: {
                type() {
                    return ALERT_TYPE.SUCCESS;
                },
                message() {
                    return 'That is success Alert';
                },
            }
        }}
    </Story>
    <Story name="warning">
        {{
            components: { Alert },
            template: `<Alert id="2" :type="type" :message="message" />`,
            computed: {
                type() {
                    return ALERT_TYPE.WARNING;
                },
                message() {
                    return 'That is warning Alert';
                },
            }
        }}
    </Story>
    <Story name="info">
        {{
            components: { Alert },
            template: `<Alert id="4" :type="type" :message="message" />`,
            computed: {
                type() {
                    return ALERT_TYPE.INFO;
                },
                message() {
                    return 'That is info Alert';
                },
            }
        }}
    </Story>
    <Story name="error">
        {{
            components: { Alert },
            template: `<Alert id="4" :type="type" :message="message" />`,
            computed: {
                type() {
                    return ALERT_TYPE.ERROR;
                },
                message() {
                    return 'That is error Alert';
                },
            }
        }}
    </Story>
</Preview>

### Props

<Props of={Alert} />
