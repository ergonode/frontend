import { Meta, Props } from '@storybook/addon-docs/blocks';
import ActionIconButton from '@Core/components/Buttons/ActionIconButton';

<Meta title="Components API|ActionIconButton" />

# ActionIconButton

`ActionIconButton` is an `ActionBaseButton` with a `IconButton` inside named slot `button`. It's implementing `IconButton` props.

```
    import ActionIconButton from '@Core/components/Buttons/ActionIconButton'
```

### Props

<Props of={ActionIconButton} />
