import { Meta, Props } from '@storybook/addon-docs/blocks';
import Select from '@UI/components/Select/Select';

<Meta title="Components API|Select" />

# Select

`Select` is a default options component. It might be configured with `prepend` and `append` slots.

```
    import Select from '@UI/components/Select/Select'
```

### Props

<Props of={Select} />
