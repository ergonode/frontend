import { Meta, Props } from '@storybook/addon-docs/blocks';
import TextArea from '@Core/components/Inputs/TextArea';

<Meta title="Components API|TextArea" />

# TextArea

`TextArea` is a default textarea input component. It might be configured with `prepend` and `append` slots.

```
    import TextArea from '@Core/components/Inputs/TextArea'
```

### Props

<Props of={TextArea} />
